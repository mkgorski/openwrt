Image with btrfs root partition:

### Prepare initalramfs rootfs
./build config-orangepizero-initrd.seed
cd bin/targets/sunxi/cortexa7
cp openwrt-sunxi-cortexa7-rootfs.cpio.gz rootfs
(cd rootfs && gunzip -c openwrt-sunxi-cortexa7-rootfs.cpio.gz | cpio -idv && rm openwrt-sunxi-cortexa7-rootfs.cpio.gz)
cd -

### Prepare final image
./build config-orangepizero.seed

### Prepare SD card
./flash
cd bin/targets/sunxi/cortexa7
diskutil mountDisk /dev/disk4
cp openwrt-sunxi-cortexa7-xunlong_orangepi-zero-initramfs-kernel.bin /Volumes/NO\ NAME/uImage
cp openwrt-sunxi-cortexa7-rootfs.tar.gz /Volumes/NO\ NAME
diskutil eject /dev/disk4

### First boot
First boot will fail due to lack of btrfs partitions. Initramfs will boot into shell. Commands to execute:
fdisk /dev/mmcblk0
mkfs.btrfs -m raid1 -d raid1 /dev/mmcblk0p3 /dev/mmcblk0p4
mount -t btrfs /dev/mmcblk0p3 /mnt
cd /mnt
mkdir /qwe
mknod /dev/mmcblk0p1 b 179 1
mount /dev/mmcblk0p1 /qwe
cp /qwe/openwrt-sunxi-cortexa7-rootfs.tar.gz .
gunzip -c openwrt-sunxi-cortexa7-rootfs.tar.gz | tar xv
rm openwrt-sunxi-cortexa7-rootfs.tar.gz
sync
reboot


....:::::::::::::::::::::::::::::::::::::::::::....

  _______                     ________        __
 |       |.-----.-----.-----.|  |  |  |.----.|  |_
 |   -   ||  _  |  -__|     ||  |  |  ||   _||   _|
 |_______||   __|_____|__|__||________||__|  |____|
          |__| W I R E L E S S   F R E E D O M
 -----------------------------------------------------

This is the buildsystem for the OpenWrt Linux distribution.

To build your own firmware you need a Linux, BSD or MacOSX system (case
sensitive filesystem required). Cygwin is unsupported because of the lack
of a case sensitive file system.

You need gcc, binutils, bzip2, flex, python3.5+, perl, make, find, grep, diff,
unzip, gawk, getopt, subversion, libz-dev and libc headers installed.

1. Run "./scripts/feeds update -a" to obtain all the latest package definitions
defined in feeds.conf / feeds.conf.default

2. Run "./scripts/feeds install -a" to install symlinks for all obtained
packages into package/feeds/

3. Run "make menuconfig" to select your preferred configuration for the
toolchain, target system & firmware packages.

4. Run "make" to build your firmware. This will download all sources, build
the cross-compile toolchain and then cross-compile the Linux kernel & all
chosen applications for your target system.

Sunshine!
	Your OpenWrt Community
	http://www.openwrt.org


